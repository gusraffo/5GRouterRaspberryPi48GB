To make a user friendly router a web interface have to be implemented so that the user can change parameters like WiFi passwords, connectivity options,
check the system operation and contact support to open tickets when a bug is found or a problem appears in the system, so the remote specialist
technitian can log into the system remotely and solve the issue.

It is not decide yet decided but FLASK can be used to interact with Python software giving a web interface for python scipts.

In the following links you can find Flask:
https://flask.palletsprojects.com/en/2.1.x/

